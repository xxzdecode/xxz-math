# xxz-math 网站规则

- 本仓库只保存数学网站业务代码，不保存练习照片、学校卷、错题详情或诊断证据。
- 网站只包含图形实验室与知识点库，不提供在线批改、错题录入或自动薄弱分析。
- 公共页面只显示通用图形和通用知识；学生状态和写操作必须进入老师模式。
- 老师模式使用一个 4 位教师 PIN，不需要用户名；PIN 不得明文进入 Git、前端配置或公开数据。
- 学生固定使用 `sister`（姐姐 / Crystal）和 `brother`（弟弟 / Gavin）。
- 交接基线、我们的教学状态与实测掌握状态必须分层显示；交接记录不能自动成为 `stable`。
- 数学私有存储、RPC 和配置 key 必须使用 `math_*` 独立命名空间。
- 未部署后端时明确显示“只读/未连接”，不得把本地假数据冒充线上结果。
- 未经授权，不部署、不执行迁移、不设置 PIN、不写生产数据、不提交或推送远端。
